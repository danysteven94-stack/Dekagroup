{
  "name": "deka-log",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "web-push": "^3.6.7",
    "pg": "^8.11.3"
  },
  "scripts": {
    "test": "node tests/run-all.js",
    "demo": "node --no-warnings tools/dev-server.js",
    "smoke": "node tools/smoke.js",
    "test:browser": "node --no-warnings tests/browser.smoke.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
